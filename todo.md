# cc-discord-remote — request log & status

Tracks every request Reuben has made for the Discord remote-control bot, with status. Newest concerns at the bottom.

## ✅ Done & verified
- **Portfolio Claude relaunch** — launched a fresh Claude in `OneDrive\Documents\Portfolio`, auto-attached a channel. (Was: portfolio chat returning nothing.)
- **Auto-spawn churn from claude-monitor scraper** — `_auto_spawn_watcher` now skips claude.exe younger than 30s (`AUTO_SPAWN_MIN_AGE_SECONDS`). Commit `8c69c89`.
- **Drop @mention on turn completions** — only approvals ping now; removed `PING_AFTER_SECONDS`. Commit `ed81240`.
- **Email digest retimed** — 16:00 (`afternoon`) + 21:00 (`evening`); renamed from morning/evening. Avoids dead overnight window.
- **Email digest embeds bullets in Discord** — action items + calendar items inline, not a file-path reference. Empty-section placeholder bullets filtered.
- **Email digest headless MCP** — `--mcp-config` added so `claude --print` actually loads workspace-mcp + ms365 (was silently missing → Gmail unscanned).
- **Terminal channels nested under `terminal` category** — all four create_text_channel sites pass `category=`. Commit `917a260`.
- **Notification settings** — answered: client-side Discord "Only @mentions" setting (no code change).

## ✅ Done — session_id rework (commit pending verification)
- **ROOT CAUSE fixed: bind by session_id, not PID** — `cmd_attach` now persists `session_id`+`cwd` (`set_identity`). `_pid_watcher`, on a dead PID, checks if the same session is alive under a NEW pid and **rebinds + restarts the mirror** instead of closing. Only a truly-gone session closes the channel. → fixes AskUserQuestion auto-close + channels dying on in-place restart.
- **Restore Claude tabs on reboot (ALL recent tabs)** — `_restore_terminals_on_boot` resumes every channel that has a session_id but no live process: `claude --resume <session_id>` in its cwd, reads back the forked session_id, re-attaches to the SAME channel. Auto-Enters trust + resume picker. Staggered 2s.
- **Reopen channels** — channels survive reboot in Discord (bot died before deleting them); restore reuses them, no churn. Orphan-delete now SKIPS channels with a session_id.
- **Duplicate messages** — auto-spawn watcher now skips a new PID whose `session_id` already has a channel (was creating a 2nd channel → 2 mirror loops → dup messages). Plus rebind cancels the old mirror.

## ⚠️ Shipped earlier, re-verify after this deploy
- AskUserQuestion eager-render (`701a402`) + line-buffered bot.log (`427cfc8`) — render confirmed working in bot.log; auto-close was the real bug (now fixed).
- Esc-before-type + picker-aware skip (`a263f94`); resume auto-Enter + auto-look (`f157b7f`); offline replay (`09453e6`).

## ✅ Done — channel protection + live-session adoption
- **Never delete human channels** — `_bot_deletable()` guard: bot only auto-deletes channels under the `terminal` category or hex-id orphans. `notifications`, `control-room`, `physics` etc. are untracked-but-never-deleted. Fixes the reboot deleting the notifications channel.
- **Adopt orphan live sessions on boot** — `_adopt_orphan_live_sessions()` gives a fresh channel to live NAMED sessions that lost theirs (survived a reboot but channel was deleted). Fixes "Discord hasn't repopulated" for still-running tabs.

## ✅ Done — duplicate-message root causes (2026-05-30)
Reuben re-reported "same message twice" after prior fixes. Found TWO real causes (bot.log showed each AskUserQuestion rendered 4×/3×/2×, and DB showed two channels per session):
- **`on_ready` re-runs on every gateway reconnect** — discord.py re-fires `on_ready` after every RESUME, and the restore/mirror block spawned a fresh `_mirror_loop` per channel each time without cancelling the old one. N reconnects → N mirror loops per channel → N copies of every message. Fixed with a one-time `_boot_done` guard (matches the existing watcher `_*_started` flags). The line `mirror_tasks[ch_id] = create_task(...)` overwrote the dict entry but left the old task alive — that was the leak.
- **Two channels bound to one session** — `_restore_terminals_on_boot` re-attached OLD leftover channels to live PIDs the `on_ready` loop had already claimed (after `claude --resume` the session_id forks, so old/new channels share a live PID). Added a `claimed_pids` guard so a PID is never attached to two channels, plus `_dedup_session_channels()` which deletes the idle duplicate when a live sibling for the same session exists. Cleaned the two existing stale channels (remote-control + unity dupes) on restart; DB now one channel per session.

## ✅ Done — message sent during compaction (2026-05-30)
- **Message typed mid-compaction came out as `/compact "my message"`** — resuming a near-full session auto-triggers Claude Code's compaction; typing into that busy input appended the message to the running `/compact`. Added `_wait_while_compacting()` — before typing a non-slash message, snapshot the screen; if "Compacting" is visible, hold (poll every 2s, max 240s) and tell the user, then deliver to the clean prompt once it clears. A deliberate `/compact` the user sends still goes through (we only hold when ALREADY compacting).

## ✅ Done — reboot-restore was dropping sessions with stale session_ids (2026-05-30)
Reuben: "after reboot it should always resume full session as is." Audit found the DB had stale session_ids — 5 of 6 tracked sessions stored an id with NO JSONL on disk, so `_restore_terminals_on_boot`'s `session_jsonl_path(...).is_file()` check would fail and silently SKIP them on reboot. Cause: Claude Code advances/forks the session_id as a conversation grows (resume, auto-compaction) and the terminals had restarted into new PIDs/sids since the last bot boot, but `_pid_watcher` only synced state when a PID *died* — never re-pinned the DB for a still-live PID. Fix: in `_pid_watcher`, for every LIVE attached PID, read the current sid from the registry (`list_running()`) and `set_identity` if it drifted (skips CONTROL_CHANNELS). Now the DB always points at the real current conversation; verified all 6 sessions DB-sid == live-sid == JSONL-exists. NOTE: `claude --resume` always forks a NEW sid (content identical, id new) — unavoidable Claude Code behavior; and resuming a near-full session triggers auto-compaction. So "as is" = full history restored, not a byte-identical session id.

## ✅ Done — reboot resumes FULL session + handles slow startup (2026-05-30)
Reuben: "always resume full session as is"; after a reboot one tab was stuck. Root causes:
- The resume picker Claude Code shows for big sessions defaults to **"Resume from summary (recommended)"** (lossy). The old restore flow sent a blind Enter → it was resuming from SUMMARY, not full. Now `_drive_resume_startup()` explicitly sends Down+Enter to pick "Resume full session as-is".
- Blind fixed-sleep Enters miss the prompt when a slow reboot (pihole/DNS lag) delays startup → tab stuck on the picker/trust screen. New helper POLLS the screen up to 90s, accepting the trust prompt and the resume picker whenever they actually appear. Wired into all 3 launch sites (cmd_resume_spawn, cmd_launch, _resume_into_existing_channel). Unstuck the 2 live stuck tabs (personalclaw, PiHole) manually via Down+Enter.

## ✅ DONE — Tabbed windows via WINDOWS TERMINAL (the right way, 2026-05-31)
Reuben showed exactly what he wants (image: his WT window with sessions as tabs) and that the tkinter tab_host was junk. KEY CORRECTION: the prior "WT tabs break AttachConsole (err 87)" conclusion was WRONG. Proof: the remote-control session itself runs inside a WT tab (parent `CASCADIA_HOSTING_WINDOW_CLASS`) and the bot scrapes+injects it fine — verified by scraping PID 29504 and getting its live screen. So WT-hosted ConPTY tabs ARE fully drivable by `AttachConsole`/`ReadConsoleOutputCharacterW`/`WriteConsoleInputW`.
- **Fix:** new `_spawn_session_window(inner_cmd, cwd, title)` launches sessions as TABS in the existing WT window via `wt -w 0 new-tab -d <cwd> --title <name> powershell -NoExit -Command <cmd>`. Replaced all 3 launch sites (cmd_resume_spawn, cmd_launch, _resume_into_existing_channel). Standalone-console fallback if wt missing.
- **wt.exe path gotcha:** bare `wt.exe` fails from the pythonw/scheduled-task bot (`FileNotFoundError`, not on PATH — it's a WindowsApps execution alias). Fixed by resolving the full `%LOCALAPPDATA%\Microsoft\WindowsApps\wt.exe` path (a symlink CreateProcess launches fine by absolute path).
- **VALIDATED end-to-end:** launched a throwaway `claude` via the exact command → appeared as a tab in Reuben's WT window (screenshot) → bot auto-spawned its channel → `console_helper --mode=look` scraped its full screen. Cleaned up (auto-closed, temp removed), no churn to real sessions.
- **`tab_host.py` (tkinter) DELETED** — superseded. autostart reverted to bot-only in `ensure-running.ps1`.
- **⚠️ bot-restart lesson:** `taskkill /T` on the bot CASCADE-kills session windows (they're child processes of the bot via CREATE_NEW_CONSOLE). Restart the bot by killing ONLY its own pids with `/F` (NO `/T`). (Killed unity+pihole once by mistake; auto-resume recovered them.)
- **MIGRATION DONE (2026-05-31):** all 6 sessions now WT tabs (unity, d1035b26, PiHole, sensor, remote-control, personalclaw). Done by closing each standalone window → bot auto-resumed it as a tab (proved the kill→auto-resume→tab path end-to-end). No litter (standalone windows close fully). Physics resumed with full history, fine.
- **Leftover from testing:** 2 DEAD WT tabs (`PATHTEST`, and a `~\AppData\Local\Temp\wttab-test` "Claude Code" tab showing "[process exited]") — can't be closed programmatically (no process to drive); Reuben must X them. Avoid in future: use self-closing test commands or `closeOnExit` config.
- **PENDING:** git commit. (3) Claude Code auto-updated again .158→.159 mid-session (cosmetic).

## (scrapped) tkinter tab_host — DO NOT REVIVE (2026-05-31)
Built and verified the free in-repo tab host. Validated the core hypothesis first (throwaway cmd test): `AttachConsole`+`ReadConsoleOutputCharacterW`+`WriteConsoleInputW` ALL keep working after a console window is `SetParent`'d into another window, and conhost renders fine as a child — because the Console API targets the buffer OBJECT, not the HWND. Then built `tab_host.py` (tkinter `ttk.Notebook`, stdlib only, no deps):
- Discovers live sessions from `~/.claude/sessions/<pid>.json`, `SetParent`s each conhost window into a tab frame. Verified: all 5 classic sessions (sensor, unity, d1035b26, PiHole, personalclaw) reparented into `TkChild` frames owned by the tab_host process; bot driving unaffected.
- **WT-hosted sessions auto-skipped** via a top-level-only guard (`GetParent(hwnd)!=0` → skip). Discovery: the remote-control session runs inside Windows Terminal (`CASCADIA_HOSTING_WINDOW_CLASS`, ConPTY) — can't be reparented, and now correctly left alone. Also takes optional argv PIDs to skip.
- **Min-age gate (30s):** don't adopt a window the bot is still steering through trust/picker. Early test caused ~3 transient session deaths (reparent mid-startup disrupted the Ink TUI); the bot's auto-resume recovered all, and the gate prevents recurrence. Re-test after hardening: zero churn.
- **Safe close:** on window close, `_on_close` re-parents every console back to the desktop FIRST (so they survive as top-level windows), THEN destroys. Verified via `taskkill` (no `/F` = WM_CLOSE): all 5 sessions survived as top-level, alive. If tab_host instead CRASHES, children die with it — but auto-resume-on-death brings them back.
- Currently RUNNING (launched via pythonw, skipping the active remote-control pid).
- **Autostart WIRED (2026-05-31):** `ensure-running.ps1` now supervises BOTH bot.py and tab_host.py (launches each if missing, on logon + every 5 min). tab_host launched visibly via pythonw, no skip-pid (WT guard handles the active window). Verified no-op when both already running. Survives reboot (GUI appears on login — verify after next reboot that the scheduled task runs in the interactive session).
- **PENDING:** Polish — per-tab resize doesn't reflow the console buffer (classic-console limit, renders at native size); optional "+launch" / "close-tab" buttons. git commit.

## 🔬 (superseded) Research — Tabbed windows ARE possible (2026-05-31)
Earlier "won't do" was WRONG — it only ruled out putting sessions INSIDE Windows Terminal tabs (that does kill `AttachConsole`, err 87). Reuben pushed back; 17-source research (agent ad3f4c4b) found the **key insight: the console BUFFER and the window HWND are decoupled.** `AttachConsole`/`ReadConsoleOutputCharacterW`/`WriteConsoleInputW` operate on the console OBJECT, independent of who owns the window. So we can consolidate at the WINDOW level while leaving each session a real classic conhost console → driving layer untouched. Two viable paths:
- **Path A — window reparenting** (`SetParent` cross-process + DWM cloak inactive tabs), the Stardock Groupy / WindowTabs technique. Groupy is shipping software that already tabs cmd/PowerShell consoles. DIY is a moderate, finicky Win32 lift with real stability risk (Raymond Chen: cross-process SetParent transitively attaches input queues → focus/Z-order/hang risk). Buy Groupy = ~zero code.
- **Path B — ConEmu adopt-existing-console** (`ConEmuC -ATTACH -CONPID=<pid>`): ConEmu is FREE, hosts REAL hidden consoles (RealConsole doc), can adopt an already-running console into a tab, and has a GuiMacro automation API. Caveat: ConEmu injects ConEmuHk.dll + hooks the console — MUST empirically verify our `AttachConsole` scraping still works after adoption.
- WT-as-multiplexer is OUT: `wt.exe` is layout-only (no read/inject API); DefTerm handoff makes apps ConPTY clients (kills attach).
- **NEXT STEP (pending Reuben go):** ~30-min empirical test — does `AttachConsole`+`ReadConsoleOutputCharacterW`+`WriteConsoleInputW` still work after (1) ConEmu `-ATTACH -CONPID`, and (2) a DIY `SetParent` into a host window? Pick whichever survives. Recommend testing ConEmu first (free, purpose-built).

## ✅ Done — Claude Code v2.1.158 broke TUI driving + overnight auto-resume gap (2026-05-31)
Reuben woke to ALL PowerShell tabs closed (laptop slept overnight), auto-resume did nothing, manual resume stuck on the trust screen, bot "not pressing Enter" anywhere. Root cause: Claude Code auto-updated to **v2.1.158** at 21:40 on 05/30 and changed two things the bot depends on, plus a latent concurrency crash surfaced. Restored all 5 dead tabs first (full history, channels re-attached), then fixed:
- **Arrow keys ignored (`console_helper.py`)** — 2.1.158's Ink/React TUI reads arrow nav from **VT escape sequences** (`ESC[B`), not synthesized `VK_DOWN` key events. `_send_single_key` now routes up/down/left/right through `_send_vt_arrow()`, injecting the escape bytes as char key-events in ONE `WriteConsoleInput` batch (split chars → lone ESC reads as cancel). Existing `"down,enter"` callers work unchanged. Proven last session via the throwaway `_vtkey.py` (selection moved); logic now byte-identical in the helper.
- **Trust screen not detected (`bot.py _drive_resume_startup`)** — 2.1.158 reworded the prompt to "Quick safety check… / Yes, I trust this folder". Old loose substrings (`"do you trust"`, `"trust the files"`) both missed the new wording AND false-fired on restored *transcript* text that mentioned trust. Now matches full phrases (`"do you trust the files"`, `"quick safety check"`, `"yes, i trust this folder"`) AND requires the live `"enter to confirm"` menu affordance, and excludes the resume-picker screen.
- **Auto-resume-on-death (`bot.py _pid_watcher`)** — THE overnight gap. When a window dies under a LIVING bot (sleep closed every console; the pythonw bot survived), the watcher only resumed if the session was alive under a NEW pid — otherwise it auto-CLOSED the channel. Now, if the session is still resumable on disk (JSONL present, cwd valid), it RESUMES into the same channel via `_resume_into_existing_channel` instead of closing. Capped at `AUTO_RESUME_MAX_ATTEMPTS=3` (then falls through to close) so a session that genuinely can't resume can't respawn a PowerShell window every 15s loop.
- **Bonus: `on_ready` crash (`RuntimeError: Set changed size during iteration`)** — the concurrent boot-restore task mutates `ALLOWED_CHANNELS` (channel adoption) while the slash-sync loop iterates it across an `await tree.sync`. Aborted the tail of `on_ready` on most reconnects. Fixed by iterating a `list(...)` snapshot. (Watchers start BEFORE this line, so they were unaffected — but it was a real crash every reconnect.)
- Deployed: killed the stale bot tree (PID 18076→child 12880, alive since 05/30 22:02, never rebooted) and relaunched via `ensure-running.ps1`. Clean startup verified — all 6 sessions re-attached, no traceback. Throwaway diag scripts (`_diag.py`, `_diag2.py`, `_vtkey.py`, `_resume_dead.py`) + temp `cc-resume` dir removed.
- **NOT yet git-committed** — pending Reuben's OK to push.
- **VT picker-driving not yet exercised live** — fresh sessions show no resume picker, so the down-arrow-into-picker path hasn't been re-confirmed against 2.1.158 end-to-end (only the identical `_vtkey.py` was). Offer a controlled picker test rather than killing a real session.

## ✅ DONE — `!cc get <path>` send files back to Discord (2026-06-04)
Reuben wanted the bot to send files (esp. text) back to him. Added `cmd_get_file` + `!cc get`/`!cc file` command: resolves `<path>` relative to the attached session's cwd (or absolute), uploads via `discord.File` to the channel. Guards: not-found, is-a-folder, empty, >25 MB (Discord cap). Added to HELP_TEXT + COMMANDS + dispatch. Deployed (no-/T restart), sessions intact. Inverse of the existing drop-file-into-channel → saves-to-cwd flow.
- **Offered next:** an "outbox" so the AGENT can push files to Discord on its own (drop file in watched dir → bot uploads to that channel), reusing the close-marker-watcher pattern. Pending Reuben's go.

## ✅ DONE — `/cc-close` instant terminal close command (2026-06-03)
Reuben wanted to close a tab from inside the Claude terminal without /exit's 15s wait + lingering tab. Built a marker-based close:
- **`~/.claude/commands/cc-close.md`** (user-level slash command) → runs `cc_close.ps1` via `!`-exec.
- **`cc_close.ps1`** (repo) — walks up the process tree to find this session's `claude.exe`, writes `close-markers/<claude_pid>` (content = parent shell pid). Doesn't kill anything itself (agent stays responsive).
- **Bot `_close_marker_watcher`** (2s poll, registered in on_ready) → on a marker matching a TRACKED session, calls `_close_session_cleanly`: untrack first (no auto-resume), `_kill_tree` claude, graceful `exit` injected to the parent shell so WT closes the tab cleanly (no zombie), delete Discord channel. Unknown/stale pids are ignored (never kill a reused pid); markers cleared on boot.
- Verified: watcher loop runs, ignores+cleans an unknown-pid marker. Real-session kill path not live-tested (would close a session) but shares the verified `_close_session_cleanly`. `close-markers/` gitignored.
- Note: existing running sessions may need a fresh start to see the new slash command; `!cc close` (Discord) and `/exit` remain as alternates.

## ✅ DONE — AskUserQuestion keypad + Enter-submit reliability (2026-06-06)
Reuben: questions don't surface a keypad (and "no reply"), and messages often don't send (Enter not pressed).
- **Keypad fix:** `_render_piece` gated the AskUserQuestion buttons on `pid_here = attached_pids.get(channel.id)` being set AT RENDER time — so whenever the channel's attachment had momentarily drifted, `buttons=False` (confirmed in bot.log: 3 renders all `buttons=False`). But the button CLICK handler already resolves the pid at click time + errors gracefully. Removed the render-time gate → keypad now shows for any single-select single-question prompt. (AskUserQuestion JSONL format verified UNCHANGED — questions[].options[].label.)
- **Enter-submit fix:** `cmd_terminal_send` (mode=type) had NO submit verification — a dropped Enter left the text in the input box unsent. Added: after typing, scrape; if the tail of the message is still in the bottom (input) area, re-press Enter (≤2 retries). Spurious extra Enter at an idle prompt is a harmless no-op. Logs `[send] Enter dropped ... re-pressing`.
- Deployed (no-/T restart). Live-tested the keypad via an AskUserQuestion to #remote-control.

## ✅ DONE — channel reconciler + close hardening (2026-06-05)
Reuben: "all terminal sessions should ALWAYS auto come up as channels", "harden the bot" (the manual-restart-vs-auto-resume race that orphaned unity — live session, no channel until a restart), and "/cc-close must also close the Discord channel".
- **Reconciler:** reworked `_auto_spawn_watcher` from "channel only NEW pids" → "ensure EVERY live session always has exactly one channel" (create+attach any live session lacking one, idempotent; min-age gate avoids racing an in-flight resume; skips `_closing_pids`). Verified: 2 passes, 0 spurious channels, no dup rows. Every ~4th pass also runs `_sweep_duplicate_terminal_channels` to collapse race dups.
- **Close hardening:** new `_closing_pids` guard set. `_close_session_cleanly` now: adds pid to `_closing_pids`, untracks, kills claude and WAITS up to ~4s for it to die (so the reconciler can't see a 'live session w/ no channel' and re-add one), graceful-exits the shell, deletes the channel; handles `ch_id=None` (orphaned session → still kill the tab). `_close_marker_watcher` (/cc-close) now closes even with no bound channel (pid came from cc_close.ps1's own walk-up, never arbitrary). `cmd_close` (!cc close) now uses the guard + DELETES the DB row (was only clearing attached_pid → stale resumable row) so close stays closed across reboot.
- Recovered unity (adopted #unity → now a tracked row) earlier; reverted the bad /exit-closes heuristic the day before (see above).

## ✅ DONE — mirror-drift fix + outbox + clean /exit restart (2026-06-04)
Three fixes in one batch (deployed, no-/T restart, sessions intact):
1. **Mirror-drift fix (the "responds but silent on Discord" bug).** Root cause: when Claude forks its session_id (auto-compaction / in-place resume), it writes to a NEW JSONL, but `_pid_watcher` only updated the DB — the `_mirror_loop` kept tailing the OLD file → permanent silence until next restart. Now, on sid drift, it cancels + restarts the mirror on the new JSONL (starting at its end, so no replay of compacted history). Most likely the intermittent bug Reuben reported.
2. **Outbox (terminal → Discord file push).** `cc_send.ps1 <path>` (walks up to find claude.exe) copies the file to `outbox/<pid>__<name>`; new `_outbox_watcher` (3s poll, registered in on_ready) uploads to that pid's channel then deletes. `/cc-send <path>` slash command (user-level) wraps it. Guards: empty / >25 MB / no-channel-after-30s-drop. **VERIFIED end-to-end** — test file landed in #remote-control. `outbox/` gitignored. (Pull side `!cc get` already existed.)
3. **Clean /exit restart.** On auto-resume after a /exit (claude dead, `-NoExit` shell still idle in its tab), inject `exit` to that recorded shell pid so WT closes the orphan tab — restart is now tidy (one new tab).

## ⛔ REVERTED — `/exit`-closes-session was WRONG (2026-06-04)
The 2026-06-03 "/exit = claude-dead + shell-alive → close" heuristic FALSE-FIRED on a claude.exe crash/auto-update (claude dies, `-NoExit` shell survives) and DELETED a live session — lost personalclaw's channel+DB row (conversation JSONL ea4ec994 was safe, 31.6 MB). Reuben's actual rule: **`/exit` should RESTART (auto-resume), not close.** Only `!cc close` (Discord) or `/cc-close` (terminal marker) truly close. Removed the heuristic from `_pid_watcher` — a dead claude now falls through to auto-resume. Recovered personalclaw + physics (resumed via wt tabs; physics came back summary-only/ctx 6% due to a false trust-Enter hitting the picker default — full history intact on disk). Renamed physics session via `/rename physics` → channel auto-renamed `#d1035b26`→`#physics` (channel-rename feature verified working LIVE).
- **TODO next:** (1) clean `/exit` restart — the auto-resume leaves the old `-NoExit` idle-shell tab orphaned; close it (inject `exit` to the recorded shell pid) on resume. (2) outbox (agent→Discord file push). (3) mirror-drift fix (responds-but-silent on Discord — likely sid-fork not restarting `_mirror_loop`).

## ✅ DONE — close a session with `/exit` (2026-06-03) [SUPERSEDED — see REVERTED above]
Solved the "can't close a tab" regression. Distinguish intentional close from accidental death via the parent PowerShell: sessions launch as `powershell -NoExit ... claude`, so a typed **`/exit`** exits claude cleanly but leaves the PowerShell ALIVE; an **X-out / sleep / machine crash** kills the PowerShell too. `_pid_watcher` now records each claude's parent PS pid while alive (`_session_shell_pid`), and on claude death: **shell still alive → user `/exit` → close** (delete channel, inject graceful `exit` to close the WT tab, DON'T resume); **shell dead → auto-resume** (recovery preserved). Edge case: a claude-only crash (app dies, shell survives) is treated as /exit (stays closed) — acceptable, rare; "computer crash" kills everything → resumes. Also after a bot reboot the dict is empty so nothing is mis-read as /exit (everything resumes). `!cc close` still works as the manual path. Deployed (no-/T restart), 7 sessions intact. Channel-rename feature also deployed same session.

## ~~🔴 NEXT TASK~~ (SUPERSEDED by the /exit fix above) — can't intentionally close a session (REGRESSION from auto-resume-on-death, 2026-05-31)
Direct consequence of fix #3 (auto-resume-on-death in `_pid_watcher`): now ANY dead PID with a JSONL on disk gets auto-resumed. So `/exit` inside Claude OR X-ing out the PowerShell window → process dies → watcher resurrects it within ~15s. Reuben has NO way to close a window he's done with. The `AUTO_RESUME_MAX_ATTEMPTS=3` cap doesn't help — each resume SUCCEEDS, which resets the counter, so it resurrects forever.
- **Need:** an explicit "I'm done with this one" path that marks the session NON-resumable BEFORE killing it, so the watcher closes (deletes channel) instead of resuming. Likely a Discord command (e.g. `!cc done` / `!cc close` / `!cc kill`) that: sets a closed flag / deletes the resumable DB row → kills the PID → deletes the channel. Watcher must check the flag and NOT auto-resume flagged sessions.
- **Consider also:** detecting a clean `/exit` (graceful Claude shutdown) vs a crash/sleep-kill, so `/exit` naturally means "stay closed" — but PID-death alone can't distinguish them, so the explicit command is the reliable fix. The `X`-out case definitely needs the command.
- Reuben: "After you fix this tab window research assignment, then this is your next task once I tell you to start on it."

## ❌ Still open
- **Catchup doesn't press Enter** — couldn't reproduce from code (`_write_text` DOES append Enter). NEEDS a concrete repro from Reuben (which message, what he saw).
- **Minimized resume windows** — offered as the WT-tabs fallback; awaiting Reuben's yes/no before changing launch behavior.
- **Channels lost in the PAST reboot** — ones already deleted with no session_id are gone as their original channels; the still-LIVE ones get fresh channels via adoption on the next restart.
- **First-restart priming** — pre-existing channels attached with OLD code lack session_id; the deploying restart re-attaches + persists it, so only reboots AFTER that are fully restorable.

## ✅ Done & verified — stale-channel auto-cleanup (2026-08-19)
- **Request: auto-clean Discord channels not open in the terminal.** Generalized `_orphan_channel_sweeper` (was hex-names-only — dead since the naming fix made every channel human-named, so orphans piled up forever). Now every 60s it deletes any Terminals-category channel with no live pid, no live session_id, and no resumable JSONL on disk. Guards: 10-min channel-age grace (create→attach handshake), 2 consecutive stale strikes (transient FS/registry blips), `_bot_deletable` (never touches human channels), attached channels left to `_pid_watcher` (auto-resume preserved). VERIFIED live 2026-08-19: swept stale #drone, #arbitrage, #user; kept all 5 live channels.
- Also committed the previously-uncommitted fixer-session work found in the tree: derived-name (`nameSource`) placeholder detection + `<base>-<hex>` dedup grouping, bot single-instance mutex, supervisor mutex in ensure-running.ps1. Mutex verified live: supervisor task fired 30s after manual launch, no double bot.
